[Unit]
Description=FastAPI service
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/taller-so-aws
ExecStart=/path/to/venv/bin/uvicorn main:app --host 0.0.0.0 --port 80
Restart=always

[Install]
WantedBy=multi-user.target
